encrypt:
  key: nuguribom

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        encrypt:
          enabled: false
        git:
          uri: https://github.com/nmrhtn7898/config-repo
          search-paths: testservice